# deque_queue.py
This Python program uses the deque data structure from the collections module to implement a queue. It adds elements to the rear of the queue and removes elements from the front using the FIFO (First In, First Out) principle.
